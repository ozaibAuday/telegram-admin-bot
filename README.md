# 🤖 بوت تليجرام لإدارة المستخدمين

بوت تليجرام متقدم لإدارة المستخدمين مع نظام أدوار متكامل (مشرف/مستخدم عادي) وقاعدة بيانات SQLite. يتضمن البوت واجهة سهلة الاستخدام مع أزرار شفافة (Inline Keyboards) وميزات إدارية شاملة.

## ✨ الميزات الرئيسية

### 👨‍💼 ميزات المشرف

- **👥 إدارة المستخدمين**: عرض قائمة كاملة بجميع المستخدمين مع تفاصيلهم
- **🔍 البحث المتقدم**: البحث عن المستخدمين باستخدام المعرّف أو الاسم أو المعرّف الفريد
- **📊 الإحصائيات**: عرض إحصائيات شاملة عن المستخدمين والنشاط
- **⛔ حظر/إلغاء الحظر**: حظر المستخدمين المزعجين أو إلغاء حظرهم
- **🔄 ترقية المستخدمين**: ترقية مستخدم عادي لمشرف
- **❌ حذف المستخدمين**: حذف المستخدمين من النظام
- **📢 الرسائل الجماعية**: إرسال رسائل لجميع المستخدمين النشطين
- **📋 السجلات**: عرض سجل كامل لجميع الأنشطة والإجراءات
- **⚙️ الإعدادات**: إدارة إعدادات النظام المختلفة

### 👤 ميزات المستخدم العادي

- **👤 الملف الشخصي**: عرض معلومات الملف الشخصي
- **📞 الدعم الفني**: التواصل مع فريق الدعم
- **📚 المساعدة**: الحصول على قائمة الأوامر المتاحة

### 🔧 الميزات التقنية

- **قاعدة بيانات SQLite**: تخزين آمن وموثوق للبيانات
- **نظام أدوار متقدم**: إدارة دقيقة للأذونات والأدوار
- **تسجيل النشاطات**: تتبع شامل لجميع الإجراءات
- **أزرار شفافة**: واجهة مستخدم سهلة وجذابة
- **معالجة الأخطاء**: نظام قوي لمعالجة الأخطاء والاستثناءات
- **Docker Support**: نشر سهل باستخدام Docker

## 🚀 البدء السريع

### المتطلبات

- **Node.js** v16 أو أحدث
- **npm** أو **yarn**
- **رمز بوت تليجرام** (احصل عليه من [@BotFather](https://t.me/BotFather))

### التثبيت

1. **استنساخ المستودع**:
```bash
git clone https://github.com/yourusername/telegram-admin-bot.git
cd telegram-admin-bot
```

2. **تثبيت المكتبات**:
```bash
npm install
```

3. **إعداد ملف البيئة**:
```bash
cp .env.example .env
```

4. **تعديل ملف `.env`**:
```env
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
ADMIN_IDS=123456789,987654321
DB_PATH=./data/bot.db
PORT=3000
NODE_ENV=development
```

### تشغيل البوت

**في بيئة التطوير**:
```bash
npm run dev
```

**في بيئة الإنتاج**:
```bash
npm start
```

## 📋 قائمة الأوامر

### أوامر المشرف

| الأمر | الوصف |
|------|-------|
| `/start` | بدء البوت والحصول على القائمة الرئيسية |
| `/help` | عرض قائمة الأوامر المتاحة |
| `/users` | عرض قائمة المستخدمين |
| `/stats` | عرض إحصائيات النظام |
| `/search <معرّف>` | البحث عن مستخدم |
| `/ban <معرّف>` | حظر مستخدم |
| `/unban <معرّف>` | إلغاء حظر مستخدم |
| `/promote <معرّف>` | ترقية مستخدم لمشرف |
| `/delete <معرّف>` | حذف مستخدم |
| `/broadcast <رسالة>` | إرسال رسالة جماعية |
| `/logs` | عرض السجلات |

### أوامر المستخدم العادي

| الأمر | الوصف |
|------|-------|
| `/start` | بدء البوت |
| `/help` | عرض قائمة الأوامر |
| `/profile` | عرض الملف الشخصي |

## 🗄️ هيكل قاعدة البيانات

### جدول `users`
```sql
- id: معرّف فريد
- user_id: معرّف المستخدم في تليجرام
- username: اسم المستخدم
- first_name: الاسم الأول
- last_name: الاسم الأخير
- role: الدور (admin/user/banned)
- status: الحالة (active/inactive/banned)
- joined_at: تاريخ الانضمام
- last_activity: آخر نشاط
- notes: ملاحظات
```

### جدول `activity_log`
```sql
- id: معرّف فريد
- user_id: معرّف المستخدم
- command: الأمر المستخدم
- description: وصف النشاط
- timestamp: الوقت والتاريخ
```

### جدول `notifications`
```sql
- id: معرّف فريد
- user_id: معرّف المستخدم
- message: نص الإشعار
- type: نوع الإشعار
- read: هل تم قراءته
- created_at: وقت الإنشاء
```

### جدول `scheduled_messages`
```sql
- id: معرّف فريد
- admin_id: معرّف المشرف
- target_user_id: معرّف المستخدم المستهدف
- message: نص الرسالة
- scheduled_time: وقت الإرسال
- sent: هل تم الإرسال
- created_at: وقت الإنشاء
```

## 📁 هيكل المشروع

```
telegram-admin-bot/
├── src/
│   ├── bot.js                 # ملف البوت الرئيسي
│   ├── commands/
│   │   ├── basicCommands.js   # الأوامر الأساسية
│   │   └── adminCommands.js   # أوامر المشرف
│   ├── db/
│   │   ├── schema.js          # تعريف قاعدة البيانات
│   │   ├── userManager.js     # إدارة المستخدمين
│   │   └── activityLogger.js  # تسجيل النشاطات
│   └── handlers/
│       └── callbackHandlers.js # معالجات الأزرار الشفافة
├── data/                      # مجلد قاعدة البيانات
├── .env.example              # مثال على ملف البيئة
├── .gitignore               # ملف تجاهل Git
├── Dockerfile               # ملف Docker
├── docker-compose.yml       # ملف Docker Compose
├── package.json             # ملف المكتبات
└── README.md               # هذا الملف
```

## 🐳 النشر باستخدام Docker

### بناء الصورة
```bash
docker build -t telegram-admin-bot .
```

### تشغيل الحاوية
```bash
docker run -d \
  -e BOT_TOKEN=YOUR_BOT_TOKEN \
  -e ADMIN_IDS=123456789 \
  -v $(pwd)/data:/app/data \
  --name telegram-bot \
  telegram-admin-bot
```

### استخدام Docker Compose
```bash
docker-compose up -d
```

## 🔐 الأمان

- **التحقق من الأدوار**: جميع الأوامر الإدارية محمية بفحص الدور
- **تشفير البيانات**: استخدام معايير الأمان الحديثة
- **معالجة الأخطاء**: معالجة آمنة لجميع الأخطاء
- **تسجيل النشاطات**: تتبع كامل لجميع الإجراءات

## 📝 ملاحظات مهمة

1. **إضافة معرّفات المشرفين**: عدّل قيمة `ADMIN_IDS` في ملف `.env` لإضافة معرّفات المشرفين
2. **الحصول على معرّفك**: أرسل `/start` للبوت للحصول على معرّفك الفريد
3. **قاعدة البيانات**: يتم إنشاء قاعدة البيانات تلقائياً عند بدء البوت
4. **النسخ الاحتياطية**: تأكد من عمل نسخ احتياطية منتظمة لملف قاعدة البيانات

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:

1. عمل Fork للمستودع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT. انظر ملف `LICENSE` للتفاصيل.

## 📧 التواصل

للأسئلة والاقتراحات:
- 📧 البريد الإلكتروني: support@example.com
- 🐛 البلاغات عن الأخطاء: [Issues](https://github.com/yourusername/telegram-admin-bot/issues)

## 🙏 شكر وتقدير

شكراً لاستخدامك هذا البوت! نتمنى أن يكون مفيداً لك.

---

**آخر تحديث**: ديسمبر 2024
